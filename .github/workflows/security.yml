name: Security Checks

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]
  schedule:
    # Run weekly on Sundays at midnight UTC
    - cron: '0 0 * * 0'

permissions:
  contents: read
  security-events: write

jobs:
  build:
    name: Build Verification
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ./src/stratum

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.26'
          cache: true
          cache-dependency-path: src/stratum/go.sum

      - name: Verify dependencies
        run: |
          go mod verify
          go mod tidy
          git diff --exit-code go.mod go.sum

      - name: Run go vet (static analysis)
        run: |
          go vet ./...

      - name: Build stratum binary
        run: |
          go build -v ./...

      - name: Run unit tests with coverage
        run: |
          go test -short -v -timeout 5m -coverprofile=coverage.out ./...

      - name: Check coverage threshold
        run: |
          COVERAGE=$(go tool cover -func=coverage.out | grep total | awk '{print $3}' | sed 's/%//')
          echo "Total coverage: ${COVERAGE}%"
          # Fail if coverage drops below 25%
          if [ "$(echo "$COVERAGE < 25" | bc -l)" -eq 1 ]; then
            echo "::error::Coverage ${COVERAGE}% is below minimum threshold of 25%"
            exit 1
          fi

      - name: Upload coverage artifact
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: src/stratum/coverage.out
          retention-days: 30
        if: always()

  security-scan:
    name: Security Scanning
    runs-on: ubuntu-latest
    needs: build

    defaults:
      run:
        working-directory: ./src/stratum

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.26'
          cache: true
          cache-dependency-path: src/stratum/go.sum

      - name: Install security tools
        run: |
          go install golang.org/x/vuln/cmd/govulncheck@latest
          go install github.com/securego/gosec/v2/cmd/gosec@latest
          go install honnef.co/go/tools/cmd/staticcheck@latest

      - name: Run govulncheck
        continue-on-error: true
        run: |
          govulncheck ./...

      - name: Run gosec
        run: |
          # G104 and G115 are excluded because they were 239 of 308 open alerts
          # and every one was triaged as a false positive, which left the code
          # scanning tab with no usable signal:
          #
          #   G104 (unhandled errors) — the WAL durability path IS fully
          #     error-checked; the hits are best-effort dir.Sync() metadata
          #     syncs and zap's logger.Sync(), which is the standard ignore.
          #   G115 (integer overflow conversion) — share counters, block
          #     heights and charset indices are all domain-bounded orders of
          #     magnitude below the conversion limits.
          #
          # Errors that matter are still caught by errcheck-style review and by
          # go vet in the build job. Do NOT add exclusions here without
          # triaging the findings first.
          gosec -no-fail -exclude=G104,G115 -fmt=sarif -out=gosec-results.sarif ./...
          gosec -no-fail -exclude=G104,G115 -fmt=text ./...

      - name: Run staticcheck
        continue-on-error: true
        run: |
          staticcheck ./...

      - name: Upload SARIF results
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: src/stratum/gosec-results.sarif
        if: always()

  race-detection:
    name: Race Detection Tests
    runs-on: ubuntu-latest
    needs: build

    defaults:
      run:
        working-directory: ./src/stratum

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.26'
          cache: true
          cache-dependency-path: src/stratum/go.sum

      - name: Run tests with race detector
        continue-on-error: true
        run: |
          go test -short -race -v -timeout 10m ./...

  fuzz-testing:
    name: Fuzz Testing
    runs-on: ubuntu-latest
    needs: build

    defaults:
      run:
        working-directory: ./src/stratum

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.26'
          cache: true
          cache-dependency-path: src/stratum/go.sum

      - name: Run fuzz tests
        run: |
          # Run each fuzz test for ~300k executions (≈30s on CI runners).
          # Using execution count instead of wall-clock time avoids spurious
          # "context deadline exceeded" failures caused by the Go fuzz engine's
          # internal context expiring at the exact fuzztime boundary.
          # -run=^$ skips all non-fuzz tests in the package (they already ran in the build job)
          go test -run=^$ -fuzz=FuzzWorkerIdentity -fuzztime=300000x -timeout=5m ./internal/shares/...
          go test -run=^$ -fuzz=FuzzDifficultyValue -fuzztime=300000x -timeout=5m ./internal/shares/...
          go test -run=^$ -fuzz=FuzzNonce -fuzztime=300000x -timeout=5m ./internal/shares/...
          go test -run=^$ -fuzz=FuzzJSONMessageParsing -fuzztime=300000x -timeout=5m ./internal/stratum/v1/...
          go test -run=^$ -fuzz=FuzzWorkerName -fuzztime=300000x -timeout=5m ./internal/stratum/v1/...

  dco-check:
    name: DCO Sign-off Verification
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Verify DCO sign-off on all commits
        run: |
          FAILED=0
          for COMMIT in $(git log --format='%H' origin/${{ github.base_ref }}..HEAD); do
            if ! git log -1 --format='%B' "$COMMIT" | grep -q '^Signed-off-by: .* <.*>'; then
              echo "::error::Commit $COMMIT is missing DCO sign-off (Signed-off-by: Name <email>)"
              FAILED=1
            fi
          done
          if [ "$FAILED" -eq 1 ]; then
            echo ""
            echo "All commits must include a DCO sign-off line."
            echo "Use: git commit -s -m \"your message\""
            echo "See DCO and CONTRIBUTING.md for details."
            exit 1
          fi
          echo "All commits have valid DCO sign-off."

  dependency-review:
    name: Dependency Review
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Dependency Review
        uses: actions/dependency-review-action@v4
        with:
          fail-on-severity: critical
          deny-licenses: GPL-3.0, AGPL-3.0
